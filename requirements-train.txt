# Reference manifest of the exact versions the verified install lands on (the core
# training stack for GrepSeek SFT + RL on verl). Pulled from the paper's conda env.
#
# This is NOT a one-shot `pip install -r` file: the verified build is ORDER-dependent
# (torch cu128 + compiled flash-attn first, then vLLM-first so it pulls a self-consistent
# tree, then --no-deps force-pins for huggingface_hub/tokenizers/safetensors and the git
# transformers). Follow the ordered recipe in TRAINING_ENV.md; use these pins to verify
# you ended up on the right versions.
#
# Installed separately in the recipe: torch/torchvision/torchaudio (cu128 index),
# flash-attn==2.8.3 and flash-linear-attention==0.4.2 (compiled). causal-conv1d
# is listed below as the reference version, but still belongs in that ordered
# compiled-kernel install step.
transformers @ git+https://github.com/huggingface/transformers.git@f048e845684894fe60440bb8506f26ffaf7b69ac
accelerate==1.13.0
causal-conv1d==1.6.1
codetiming==1.4.0
datasets==4.3.0
deepspeed==0.16.7
dill==0.4.0
einops==0.8.2
fastapi==0.135.1
flashinfer-python==0.6.4
huggingface_hub==1.8.0
hydra-core==1.3.2
msgspec==0.20.0
numpy==2.3.5
omegaconf==2.3.0
outlines==0.1.11
packaging==26.0
pandas==3.0.1
peft==0.18.1
pyarrow==23.0.1
pydantic==2.12.5
ray==2.54.0
safetensors==0.7.0
sentencepiece==0.2.1
tensordict==0.12.2
tiktoken==0.12.0
tokenizers==0.22.2
torchdata==0.11.0
uvicorn==0.41.0
vllm==0.17.0
wandb==0.25.1
xformers==0.0.35
